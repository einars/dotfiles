#!/usr/bin/env bash

VIDEO_IN=$1
VIDEO_OUT=$2

if [ "$2" = "" ]; then
    VIDEO_OUT="${VIDEO_IN}.avi"
fi;

echo from $VIDEO_IN to $VIDEO_OUT

if [ "$VIDEO_IN" = "" ]; then 
    echo $0 input [output]
else 
    ffmpeg  -i "$VIDEO_IN" -b 1300k "$VIDEO_OUT"
fi;

